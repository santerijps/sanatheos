pre-commit:
  parallel: true
  commands:
    lint:
      run: bunx oxlint src/ tests/ scripts/
    fmt-check:
      run: bunx oxfmt --check src/ tests/ scripts/
    typecheck:
      run: bunx tsc --noEmit

pre-push:
  commands:
    test:
      run: bun test
