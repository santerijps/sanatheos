{
  "name": "bible-app",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun run --watch src/server.ts",
    "build:static": "bun run scripts/build-static.ts",
    "download-translation": "python scripts/bible_gateway.py"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
